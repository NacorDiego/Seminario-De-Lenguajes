import HeaderComponent from '../../components/HeaderComponent'

const DashboardPage = () => {
  return (
    <>
      {/* Aca vamos a controlar todas los headers y nav */}
      <HeaderComponent />
      {/* Y aca vamos a poner el listado de pages que se van a rendereizar segun el boton que toques en el navbar la idea es usar react-router-dom */}
      {/* Aca abajo agregar el footer */}
    </>
  )
}

export default DashboardPage
